namespace BusinessWallet.models.Enums
{
    public enum EmployeeState
    {
        Inactive = 0,   // ⬅️  default (zero-waarde)
        Active = 1,
        OnLeave = 2,
        Suspended = 3,
        Terminated = 4,
        Retired = 5
    }
}
